let colors = ['red', 'green', 'blue', 'yellow'];
let removedLast = colors.pop();
let removedFirst = colors.shift();
console.log(colors); // Correct
console.log('Removed:', removedLast, removedFirst); // Typo in second variable
