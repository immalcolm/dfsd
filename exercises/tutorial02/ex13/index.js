// Step 1: Initialize an array containing several elements
let items = ['apple', 'banana', 'cherry', 42, true];

// Step 2: Use a for...of loop to iterate through the array
for (const item of items) {
    // Step 3: Print each element
    console.log(item);
}
