// Step 1: Choose a number for the multiplication table
let number = 7; // You can change this number to generate a different table

// Step 2: Use a for loop to compute and print the multiplication table from 1 to 10
for (let i = 1; i <= 10; i++) {
    // Step 3: Compute and print each line of the table
    console.log(`${number} x ${i} = ${number * i}`);
}
